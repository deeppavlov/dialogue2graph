dialogue2graph.metrics.no_llm_validators.validators
===================================================

.. py:module:: dialogue2graph.metrics.no_llm_validators.validators

.. autoapi-nested-parse::

   Validators
   --------------------------

   The module contains validators to evaluate dialogs with regular expressions.



Functions
---------

.. autoapisummary::

   dialogue2graph.metrics.no_llm_validators.validators.is_greeting_repeated_regex
   dialogue2graph.metrics.no_llm_validators.validators.is_dialog_closed_too_early_regex


Module Contents
---------------

.. py:function:: is_greeting_repeated_regex(dialogs: List[dialogue2graph.pipelines.core.dialogue.Dialogue], regex: str = None) -> bool

   Check if greeting is repeated within dialogues using regular expression.

   :param dialogs: Dialog list from graph.
   :type dialogs: List[Dialogue]
   :param regex: Regular expression to find start turns. Defaults to None, so standard regex is used.
   :type regex: str

   Returns
       bool: True if greeting has been repeated, False otherwise.


.. py:function:: is_dialog_closed_too_early_regex(dialogs: List[dialogue2graph.pipelines.core.dialogue.Dialogue], regex: str = None) -> bool

   Check if assistant tried to close dialogue in the middle using regular expression.

   :param dialogs: Dialog list from graph.
   :type dialogs: List[Dialogue]
   :param regex: Regular expression to find end turns. Defaults to None, so standard regex is used.
   :type regex: str

   Returns
       bool: True if closing appeared too early, False otherwise.


